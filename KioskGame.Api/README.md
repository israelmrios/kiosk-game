## Getting Started

1. Install .NET 8 SDK.
2. From repo root: `dotnet tool restore`
3. Run the API: `dotnet run --project KioskGame.Api/KioskGame.Api.csproj`
4. Open Swagger: `http://localhost:5298/swagger`
5. Frontend allowed origin: `http://localhost:5173`
